name: scheduled-workflow

on:
  schedule:
    - cron: '*/5 * * * *'   # Every 5 minutes

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: |
          echo "Hello World"
          exit 0
